#pragma once

// #define DEFAULT_INPROC_LIB_PATH "/usr/lib/libinproc_inject.so"
#define INPROC_AUDIT_ENV_VAR "INPROC_AUDIT_ENV_VAR"
#define DEFAULT_INPROC_AUDIT_LIB_PATH "build/libinproc_audit.so"

#define INPROC_PRELOAD_ENV_VAR "INPROC_PRELOAD_ENV_VAR"
#define DEFAULT_INPROC_PRELOAD_LIB_PATH "build/libinproc_preload.so"

#define BUFSIZE 9000
